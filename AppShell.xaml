<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    Shell.FlyoutBehavior="Disabled"
    x:Class="AcuPuntos.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:local="clr-namespace:AcuPuntos.Views"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Shell.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Gray950}}" StatusBarStyle="LightContent" />
    </Shell.Behaviors>



    <!--  Página de Splash (inicial)  -->
    <ShellContent
        ContentTemplate="{DataTemplate local:SplashPage}"
        Route="splash"
        Title="Splash" />

    <!--  Página de Login (sin tabs)  -->
    <ShellContent
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="login"
        Title="Login" />

    <!--  Páginas principales con tabs  -->
    <TabBar Route="main">
        <Tab Route="home" Title="🏠 Inicio">
            <ShellContent ContentTemplate="{DataTemplate local:HomePage}" />
        </Tab>

        <Tab Route="transfer" Title="💸 Transferir">
            <ShellContent ContentTemplate="{DataTemplate local:TransferPage}" />
        </Tab>

        <Tab Route="rewards" Title="🎁 Recompensas">
            <ShellContent ContentTemplate="{DataTemplate local:RewardsPage}" />
        </Tab>

        <Tab Route="agenda" Title="📅 Agenda">
            <ShellContent ContentTemplate="{DataTemplate local:AgendaPage}" />
        </Tab>

        <Tab Route="profile" Title="👤 Perfil">
            <ShellContent ContentTemplate="{DataTemplate local:ProfilePage}" />
        </Tab>
    </TabBar>

</Shell>
